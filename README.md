# PySpark JSON Config Reader

This is a simple PySpark application that reads a JSON file as a config, processes data, and writes output.

## Setup

### Build the Docker Image
```sh
docker-compose build